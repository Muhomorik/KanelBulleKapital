<UserControl
    x:Class="FikaFinans.Wpf.Views.ModelResultPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:FikaFinans.Wpf.Views.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FikaFinans.Wpf.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:ModelRunViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="500"
    d:DesignWidth="380"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:RunStatusToBrushConverter x:Key="RunStatusToBrush" />
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Border
        Padding="12"
        Background="{DynamicResource MahApps.Brushes.Control.Background}"
        BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}"
        BorderThickness="1"
        CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header: status dot + model name  -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Ellipse
                    Grid.Column="0"
                    Width="10"
                    Height="10"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Fill="{Binding Status, Converter={StaticResource RunStatusToBrush}}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Text="{Binding ModelId}" />
            </Grid>

            <!--  Elapsed counter / progress  -->
            <Grid Grid.Row="1" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <mah:ProgressRing
                    Grid.Column="0"
                    Width="14"
                    Height="14"
                    Margin="0,0,8,0"
                    IsActive="{Binding IsRunning}"
                    Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Opacity="0.7"
                    Text="{Binding ElapsedLabel}" />
            </Grid>

            <!--  Token counts  -->
            <TextBlock
                Grid.Row="2"
                Margin="0,0,0,8"
                Opacity="0.7">
                <Run Text="in: " /><Run Text="{Binding InputTokens, Mode=OneWay, StringFormat={}{0:N0}}" /><Run Text="   out: " /><Run Text="{Binding OutputTokens, Mode=OneWay, StringFormat={}{0:N0}}" />
            </TextBlock>

            <!--  Response (read-only TextBox so Ctrl+A / Ctrl+C work natively)  -->
            <Grid Grid.Row="3">
                <TextBox
                    AcceptsReturn="True"
                    Background="Transparent"
                    BorderThickness="0"
                    FontFamily="Consolas, Cascadia Code, Courier New"
                    FontSize="12"
                    HorizontalScrollBarVisibility="Disabled"
                    IsReadOnly="True"
                    Text="{Binding ResponseText, Mode=OneWay}"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    Visibility="{Binding IsError, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />
                <TextBlock
                    Foreground="{Binding Status, Converter={StaticResource RunStatusToBrush}}"
                    Text="{Binding ErrorText}"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}" />
            </Grid>

            <Grid Grid.Row="4" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    Padding="14,4"
                    Command="{Binding RunCommand}"
                    Content="Run this model"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}" />
                <Button
                    Grid.Column="2"
                    Command="{Binding CopyResponseCommand}"
                    Content="Copy response" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
