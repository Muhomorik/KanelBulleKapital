<mah:MetroWindow
    x:Class="FikaFinans.Wpf.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FikaFinans.Wpf.ViewModels"
    Title="FikaFinans — Settings"
    Width="640"
    Height="420"
    d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel,
                                     IsDesignTimeCreatable=True}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="14"
            Text="Data folder — where the canonical fund-data files live (summary.csv, metadata.csv, positions.csv, portfolio_structure.md, analytics-rotation-targets.md, analytics-substitution-chain.md, analytics-weekly-summary.md)."
            TextWrapping="Wrap" />

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="0,0,8,0"
                VerticalContentAlignment="Center"
                Text="{Binding DataFolder, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="1"
                Padding="14,4"
                Command="{Binding BrowseCommand}"
                Content="Browse…" />
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock
                FontSize="11"
                Opacity="0.7"
                Text="Settings file:" />
            <TextBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="Consolas"
                FontSize="11"
                IsReadOnly="True"
                IsReadOnlyCaretVisible="False"
                Opacity="0.85"
                Text="{Binding SettingsFilePath, Mode=OneWay}" />
            <TextBlock
                Margin="0,8,0,0"
                FontSize="11"
                Opacity="0.7"
                Text="Foundry uploads sidecar:" />
            <TextBox
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="Consolas"
                FontSize="11"
                IsReadOnly="True"
                IsReadOnlyCaretVisible="False"
                Opacity="0.85"
                Text="{Binding FoundryFilesPath, Mode=OneWay}" />
        </StackPanel>

        <TextBlock
            Grid.Row="3"
            VerticalAlignment="Top"
            Foreground="{DynamicResource MahApps.Brushes.Accent}"
            Text="{Binding ValidationMessage}"
            TextWrapping="Wrap" />

        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                MinWidth="90"
                Margin="0,0,8,0"
                Command="{Binding CancelCommand}"
                Content="Cancel"
                IsCancel="True" />
            <Button
                MinWidth="90"
                Command="{Binding SaveCommand}"
                Content="Save"
                IsDefault="True"
                Style="{DynamicResource MahApps.Styles.Button.Square.Accent}" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
