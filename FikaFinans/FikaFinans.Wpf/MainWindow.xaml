<mah:MetroWindow
    x:Class="FikaFinans.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FikaFinans.Wpf.ViewModels"
    xmlns:views="clr-namespace:FikaFinans.Wpf.Views"
    Title="{Binding Title}"
    Width="1500"
    Height="1000"
    d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button
                Command="{Binding OpenSettingsCommand}"
                Content="⚙"
                FontSize="18"
                ToolTip="Settings — data folder location" />
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <TabControl>
        <TabItem Header="Compare Models">
            <views:CompareModelsView DataContext="{Binding CompareModelsViewModel}" />
        </TabItem>
    </TabControl>
</mah:MetroWindow>
