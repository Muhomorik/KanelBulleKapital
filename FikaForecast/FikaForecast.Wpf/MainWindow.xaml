<mah:MetroWindow x:Class="FikaForecast.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:views="clr-namespace:FikaForecast.Wpf.Views"
        xmlns:viewModels="clr-namespace:FikaForecast.Wpf.ViewModels"
        xmlns:converters="clr-namespace:FikaForecast.Wpf.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Title="FikaForecast" Height="700" Width="1200"
        MinHeight="500" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Icon="fikaforecast.ico">

    <mah:MetroWindow.Resources>
        <converters:ResponsiveTabHeaderConverter x:Key="ResponsiveTabHeaderConverter" />
    </mah:MetroWindow.Resources>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Content="&#x1F4E4; Export" Click="OnExportClick"/>
            <Button Content="&#x2699; Settings" Click="OnSettingsClick"/>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <mah:MetroTabControl mah:TabControlHelper.Underlined="SelectedTabItem">

        <TabItem Padding="16,8">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x2194;  Comparison" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:ComparisonView DataContext="{Binding ComparisonVm}" />
        </TabItem>

        <TabItem Padding="16,8" IsSelected="{Binding HistoryVm.IsActive, Mode=TwoWay}">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F552;  History" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:HistoryView DataContext="{Binding HistoryVm}" />
        </TabItem>

        <TabItem Padding="16,8">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F50D;  Evaluation" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:EvaluationView DataContext="{Binding EvaluationVm}" />
        </TabItem>

        <TabItem Padding="16,8" IsSelected="{Binding WeeklySummaryVm.IsActive, Mode=TwoWay}">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F4CA;  Weekly" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:WeeklySummaryView DataContext="{Binding WeeklySummaryVm}" />
        </TabItem>

        <TabItem Padding="16,8" IsSelected="{Binding SubstitutionChainVm.IsActive, Mode=TwoWay}">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F517;  Chains" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:SubstitutionChainView DataContext="{Binding SubstitutionChainVm}" />
        </TabItem>

        <TabItem Padding="16,8" IsSelected="{Binding OpportunityScanVm.IsActive, Mode=TwoWay}">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F3AF;  Targets" />
                    <Binding Source="False" />
                </MultiBinding>
            </TabItem.Header>
            <views:OpportunityScanView DataContext="{Binding OpportunityScanVm}" />
        </TabItem>

        <TabItem Padding="16,8" Margin="16,0,0,0" IsSelected="{Binding BatchVm.IsAutoSchedule, Mode=OneTime}">
            <TabItem.Header>
                <MultiBinding Converter="{StaticResource ResponsiveTabHeaderConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Source="&#x1F4C5;  Batch" />
                    <Binding Source="True" />
                </MultiBinding>
            </TabItem.Header>
            <views:BatchView DataContext="{Binding BatchVm}" />
        </TabItem>
    </mah:MetroTabControl>
</mah:MetroWindow>
